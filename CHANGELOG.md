# Changelog

本文件记录 [Electron 桌面版](README.md) 的变更，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

> 关于页展示的更新日志来自 `src/shared/changelog.ts`，发布新版本时请同步维护两处。

## [Unreleased]

---

## [1.3.0] - 2026-06-12

### Added

- **歌词查看**：元数据块艺人头像点击可放大预览，大图下方显示对应人名
- **文件名与标签**：检测作者名误用 `_` 分隔（如 `李雨霏 _ 晚饭`，`_` 两侧空格数量不限）；若与 ` & ` 或单独列出的作者重复，可批量去掉 `_` 写法；修正 `_` 分隔时先写入规范作者再去掉原 `_` 项，文件名优先于标签

### Changed

- **乐库管理**：目录统计从左侧树底移至右侧列表底部靠右展示
- **列表**：VirtualDataTable 去掉 bordered wrapper 圆角；乐库管理、LRC 归位、文件名与标签、音乐解密等页列表区域去掉多余内边距，贴边展示
- **LRC 歌词归位**：Tab 栏保留左侧 10px 间距
- **文件名与标签**：无数据时空状态仅保留文字，去掉圆角背景框
- **音乐解码**：文件列表与结果 Tab 列表布局与乐库管理对齐

### Fixed

- **批处理**：修复不确定进度（如统计文件总数前）显示为 100% 再回跳的问题
- **文件名与标签**：修复再次扫描时进度条短暂沿用上次 100% 的问题

---

## [1.2.9] - 2026-06-05

### Added

- **元数据编辑**：扩展 / 其它标签分 Tab 展示，支持逐条编辑、删除；写入时合并扩展原生字段
- **元数据编辑**：支持 OGG / Opus、M4A / MP4 / ALAC 标签写入（Vorbis Comment / iTunes ilst）
- **文件名与标签**：检测扩展标签重复（同键 / ARTIST+ARTISTS 别名）与扩展字段、文件名繁体，支持批量清理与繁转简
- **文件名与标签**：检测 MP3 文件尾 ID3v1 标签（常为 `????`），支持批量删除（保留 ID3v2）
- **文件名与标签**：扫描进度与用时；左下角全局进度条展示已扫描 / 已对比数量
- **乐库同步**：对比过程显示左右目录扫描与路径对比进度及用时
- **批处理**：文件名与标签扫描、乐库对比等任务通过 IPC 上报阶段性进度
- **关于**：新增「音频知识」Tab，介绍 ID3v1 / ID3v2 / Vorbis Comment、常见格式（MP3 / FLAC / ALAC 等）及码率、采样率、位深
- **播放**：播放前检测编码；ALAC、WMA、APE 等内置播放器不支持的格式给出明确提示
- **LRC 歌词归位 / 文件名与标签 / 乐库同步**：扫描前侧栏展示用途与使用说明

### Changed

- **导航**：启动默认进入「乐库管理」页
- **文件名与标签**：进入页面或更换目录不再自动扫描，需手动点击「开始扫描」
- **文件名与标签**：扫描改为先全量读取标签再在内存对比，跳过封面 / 时长以加快大库扫描
- **乐库同步**：进入页面或更换目录不再自动对比，需手动点击「扫描对比」
- **LRC 歌词归位**：恢复「删除选中多余歌词 / 音频 (N)」按钮文案
- **MP3 写入**：写入或修复标签时自动移除文件尾 ID3v1，避免与 ID3v2 中文内容冲突
- **FLAC 写入**：改用手动拼装标签块与安全封面读取，降低删封面后误报损坏的概率

### Fixed

- **批处理**：修复异步扫描 / 对比任务取消无效的问题
- **批处理**：各页面进度条互不覆盖（按任务 owner 隔离）
- **元数据**：标签解析失败时尝试多种 read 选项；损坏 FLAC / OGG / M4A 写入前拦截并提示

---

## [1.2.8] - 2026-06-04

### Added

- **歌词查看**：LRC 归位、音乐解密、乐库管理等列表点击「有」标签可弹窗预览同级歌词；支持 LRC 文件头（标题/歌手/专辑等）与网易云 JSON 元数据（作词/作曲/编曲及艺人头像）
- **歌词查看**：时间轴与正文分色展示，元数据块与歌词正文以分隔线区分
- **乐库管理**：目录树工具栏可切换「仅当前目录 / 含全部子文件夹」列表范围
- **重复清理**：识别同名编号副本（如 `A.flac` 与 `A(1).flac`）；封面对比弹窗横向并排展示，固定封面尺寸
- **文件名与标签**：按艺人分隔规范检测（文件名用 `,`、标签用 ` & `）；按问题类型筛选，每项可单独「执行」；有选中时仅处理选中且符合条件的记录
- **文件名与标签**：检测扩展名前尾下划线（如 `曲名_.flac`）及文件名 / 标签艺人、曲名首尾 `_`，合并为「文件名下划线」「标签下划线」两类并支持批量修复
- **列表**：各音频列表右击可选「打开文件所在位置」
- **批处理**：长时间任务支持取消；左下角悬浮进度面板（毛玻璃背景）统一展示进度与取消按钮

### Changed

- **导航**：顶栏功能页「标签校准」更名为「文件名与标签」
- **文件名与标签**：文件名艺人重命名若目标已存在，自动追加 `(1)`、`(2)`… 后缀；批量进度不再显示「上个约 / 本首约」用时
- **元数据面板**：多选时不显示，仅单选一条时展示
- **重复清理 / 文件名与标签**：扫描来源下拉框占满侧栏宽度
- **乐库同步**：对比与复制过程显示进度，单文件复制也显示进度
- **界面**：Tooltip / Popover 使用 12px 小字
- **重复清理**：组内重复项改为纵向列表；切换保留项时封面对比不再跳动

### Fixed

- **界面**：修复部分页面 `useMessage` 缺少 Provider 导致报错
- **批处理**：修复进度长期停留在「正在处理」、乐库同步复制时无进度反馈

---

## [1.2.7] - 2026-06-04

### Added

- **元数据面板**：新增 `AudioMetaPanelSection` 组合组件（路径栏 + 元数据）；文件名与标签、乐库同步选中音频后可在侧栏查看并编辑标签
- **文件名与标签**：批量写入标签时显示进度条与耗时预估
- **乐库同步**：差异列表头部增加全选与全折叠

### Changed

- **元数据面板**：未选中文件时不显示；扫描、写入或同步复制 / 删除过程中自动隐藏
- **乐库同步**：复制到两侧按钮并排一行；移除「取消选择」与底部使用说明；恢复「删除选中」按钮文案
- **音乐解密**：结果区改用 `AudioMetaPanelSection`

---

## [1.2.6] - 2026-06-03

### Added

- **顶栏播放器**：展示封面、进度与播放控制；乐库管理、LRC 歌词归位、音乐解密列表双击播放明文音频；松手后再 seek
- **元数据编辑**：MP3 / FLAC 支持在侧栏元数据面板打开编辑弹窗，写入常见标签与封面；支持从文件名解析艺人 / 曲名
- **文件名与标签**：扫描「艺人 - 曲名」文件名与内嵌标签不一致的音频；支持批量用文件名合并写入标签

### Changed

- **元数据面板**：标签 Tab 间距收紧；专辑封面增加细边框；批量删除按钮改为图标 + 数量（重复清理页仍保留「删除副本」文案）
- **编辑弹窗**：输入框与布局略放大，便于填写标签
- **列表状态标签**：乐库 / LRC / 解密 / 文件名与标签等页统一为圆角轻量标签（sv-pill），虚拟列表滚动更流畅
- **界面**：配置 Naive UI 中文 locale，确认框按钮显示「确定」/「取消」

### Fixed

- **应用图标**：修正 Windows 等平台图标显示
- **文件名与标签**：列表区域铺满可用高度；批量写入改为合并艺人/曲名且跳过过大封面，避免写入失败

---

## [1.2.5] - 2026-06-02

### Added

- **重复清理**：封面对比弹窗，展示各副本封面、音频参数与封面元数据，点击卡片选择保留项

### Changed

- **重复清理**：列表增加封面缩略图，各副本展示大小、比特率、采样率、位深
- **高级功能**：解锁码输入改为 `*` 掩码，文案统一为「解锁码」
- **设置**：Tab「同步设置」「高级功能」简化为「同步」「高级」
- **解密说明**：补充 QQ 音乐 Windows v19.51 推荐版本与 Telegram 下载链接
- **打包**：镜像改由 `.npmrc` 配置，打包脚本跨平台可用；包名改为 `songvault`（安装目录同步）
- **README**：更新截图与打包说明，公开文档不再列出音乐解码

### Fixed

- **Windows 打包**：修复 cmd 下环境变量写法无效，以及 winCodeSign 符号链接权限导致的打包失败

---

## [1.2.4] - 2026-06-01

### Added

- **重复清理**：扫描乐库内同名重复文件（大小可不同），树形分组展示并支持批量删除
- **高级功能**：设置 → 高级中输入访问码解锁音乐解码、解密说明等（解锁状态可持久化）

### Changed

- **乐库同步**：切换导航后保留上次对比结果与差异树，返回时直接展示；手动「扫描对比」时 loading 即时反馈
- **乐库同步 / 音乐解码**：目录树工具栏样式与乐库管理一致（pane-head 底边线）
- **应用品牌**：展示名称简化为「乐库管理」
- **乐库管理**：无效乐库根目录在树节点标注「路径不存在」，校验完成前不再误请求子目录
- **LRC 歌词归位**：源歌词列改用省略展示，悬停查看完整路径
- **重复清理**：扫描源下拉宽度随内容自适应

### Fixed

- **乐库管理**：文件列表补全文件大小列数据
- **LRC 歌词归位**：无可选源歌词时的展示与交互

---

## [1.2.3] - 2026-05-30

### Added

- **开源致谢**：关于页新增「开源致谢」标签，展示 Unlock Music 项目信息与 MIT 许可全文（读取 `src/unlock-music/LICENSE`）
- **许可证**：仓库根目录添加 MIT `LICENSE`

### Changed

- **命名统一**：界面与文档中「曲库」统一为「乐库」；顶栏「音频库」更名为「乐库管理」
- **设置文案**：路径页「音频搜索目标」改为「你的乐库目录」；LRC 源与乐库同步相关说明优化
- **设置样式**：路径页底部链接默认更易读，悬停时高亮

---

## [1.2.2] - 2026-05-29

### Added

- **乐库同步**：支持删除选中差异项（左右乐库音频及同名歌词）；批量/单项同步或删除完成后侧栏展示结果气泡；左右对比统计面板
- **乐库同步**：进入页面与扫描前校验左右目录是否存在，无效时在页面内提示而非弹出系统错误
- **表格显示**：设置中可调整文件列表字号（行高随字号自动收紧），同屏显示更多内容
- **耗时提示**：乐库同步批量复制展示已用时间、单文件耗时与剩余估算；LRC 扫描告警面板展示扫描/匹配/删除用时

### Changed

- **应用品牌**：更名为「乐库管理 `songvault`）；更新窗口标题、打包标识与说明文案
- **工程结构**：各工作台页面组件归类至 `pages/` 子目录
- **音乐解码**：目录文件列表默认按「库中已有」排序，尚未入库的文件靠前；解码列表默认不再展示「创建时间」列

---

## [1.2.1] - 2026-05-28

### Added

- **乐库同步**：顶栏新增工作台，可在设置中指定左右乐库目录与别名；按相对路径对比音频差异（相同、仅一侧、大小不同、已移动），树形展示并支持 Shift 多选与批量同步
- **乐库同步操作**：仅一侧有文件时跨库复制；识别为已移动时在目标乐库内 rename 对齐路径（同名歌词一并移动）；对比扫描仅包含音频文件

### Changed

- **LRC 预览匹配**：一次扫描目标目录并缓存文件名列表，避免重复 `readdir`；目录未变时再次预览可复用缓存；执行复制或删除多余文件后自动失效
- **界面布局**：各工作台布局调整；选中文件路径改在底部信息条展示
- **音频元数据**：无封面时显示占位图

### Fixed

- **音频库**：在「移动到文件夹」弹窗内新建子目录后，移动完成时同步左侧目录树

---

## [1.2.0] - 2026-05-28

### Added

- **应用标识**：窗口与关于页展示 Logo
- **顶部主导航**：LRC 歌词归位、音乐解码、音频库、解密说明、设置、关于；各工作台通过顶栏切换，移除页内返回按钮
- **关于页**：展示应用名称、版本号及更新日志（与 `CHANGELOG.md` 同步）
- **解密说明页**：独立页面展示 QQ 音乐 / 网易云下载与解密说明（替代解码页帮助弹窗）
- **音频元数据面板**：LRC、音频库、音乐解码侧栏底部固定展示选中文件的封面与标签（常规 / Vorbis / MusicBrainz / 扩展信息 Tab）
- **解码页文件信息条**：选中待解密文件时显示路径等摘要
- **音频库**：列表支持按文件名搜索；支持 Shift 多选、批量删除与移动到目标文件夹（目录树选择，可新建子文件夹）
- **LRC 扫描**：支持处理「多余 LRC」「缺少音频」等异常配对（扫描告警与结果面板增强）

### Changed

- **大列表性能**：源文件、解码队列、LRC 结果等表格统一使用虚拟滚动
- **设置面板**：布局优化；文件列表列可配置更多标签类显示项
- **系统菜单**：帮助菜单跳转「解密说明」；Windows 增加「关于」入口

### Removed

- 列表悬停弹出式元数据面板及相关设置项（改为侧栏固定面板）

### Fixed

- QQ 音乐部分曲目无法解密的问题
- 网易云（NCM）曲目无封面时解密失败；兼容 v3 艺术家元数据格式，无封面时跳过易失败的标签嵌入

---

## [1.1.0] - 2026-05-27

### Changed

- **工程结构**：将原 `electron/` 子项目扁平化至仓库根目录（`src/`、`package.json`、`electron.vite.config.ts` 等），与常规 electron-vite 布局一致；更新 README 与 `.gitignore`

---

## [1.0.0] - 2026-05-26

### Added

- **音乐解码**：集成 unlock-music 解密能力，支持网易云、QQ 音乐等加密格式批量解密；支持批量处理任意数量文件；解密结果尽可能保留源文件全部元数据
- **音乐解码说明**：解码页提供「下载与解密说明」帮助弹窗
- **音乐解码队列**：列表显示目标路径是否已存在同名音频文件
- **打包**：electron-builder 脚本，可构建 Windows / macOS 安装包
- **文件列表音频信息**：源文件页与解码页列表支持比特率、时长、采样率、标签等列；可在「设置 → 文件列表列」中配置显示项（解码列表不含无法读取的标签/音频参数列）
- **文件过滤规则**：扫描与目录浏览时按名称跳过指定文件/文件夹；设置页可编辑规则
- **设置界面**：主题（浅色/深色）、音乐解码浏览目录、文件列表列、名称过滤等
- **源文件管理**：目录树浏览、新建/重命名/删除文件夹、删除选中音频、同级 LRC 显示
- **布局**：侧栏与表格高度随窗口尺寸更新；默认窗口宽高比 2:1（1520×760，高度基准 760）

### Changed

- 解密页待解密队列与文件列表区域高度优化
- 目录树：结构变化后保留展开状态；子文件夹少于 10 个时默认展开
- 列表「状态」「平台」「同级 LRC」等列内容居中显示

### Fixed

- 删除多余 LRC 文件时的错误（`fix delete lrc file error`）
- 文件管理中删除子文件夹时渲染进程误用 Node `path` 导致报错的问题（改为 `pathKeys` 纯字符串比较）
- 解码后的 FLAC 文件缺少解密前元数据的问题

---

## [0.2.0] - 2026-05-25

### Added

- LRC 与音频匹配预览、批量复制歌词到新界面（Vue 3 + Naive UI）
- 扫描结果分 Tab 展示（全部 / 已匹配 / 可复制 / 待选源等）

---

## [0.1.0] - 2026-05-25

### Added

- Electron + electron-vite 工程骨架
- 初始仓库与 Node.js 命令行版脚本（`nodejs/`）
